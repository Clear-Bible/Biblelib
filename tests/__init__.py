"""Test suites.

For continuous testing of code (and tests) in a module, install `entr`
and run this (example for `tests`):

$ ack --type=python biblelib tests -f | entr -r poetry run pytest tests

"""
